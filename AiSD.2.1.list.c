#include <stdio.h>

struct list{ // stryktura
	int key;
	struct list *next;
};

struct list *insert(struct tree *elementNew, int x) { //budowa drzewa
		list=(struct tree*)malloc(sizeof(struct tree));
		list->key = x;
		list->next = ;
		return(root);
	
	return(root);
}

int main (void)
{
	return 0;
}
